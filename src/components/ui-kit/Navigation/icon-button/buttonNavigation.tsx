import { FC } from 'react';
import styles from './buttonNavigation.module.scss';

// отображение иконок из header

export const ButtonNavigation: FC = () => (
  <>
    {/* <Badge color='error'> 
          <IconButton aria-label='like' className={styles.buttonNav}>
            <Person htmlColor='white' sx={{ width: '22px', height: '22px' }} />
          </IconButton>
        </Badge>
        <Badge
          badgeContent={1}
          color='error'
          showZero={false}
        >
          <IconButton aria-label='like' className={styles.buttonNav}>
            <ShoppingBag
              htmlColor='white'
              sx={{ width: '22px', height: '22px' }}
            />
          </IconButton>
        </Badge>
        <Badge badgeContent={2} color='error' showZero={false}>
          <IconButton aria-label='like' className={styles.buttonNav}>
            <Favorite
              htmlColor='white'
              sx={{ width: '22px', height: '22px' }}
            />
          </IconButton>
        </Badge> */}
  </>
);
