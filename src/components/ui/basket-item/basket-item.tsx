import { FC, memo } from 'react';
import styles from './basket-item.module.css';

export const BasketItemUI: FC = memo((product) => (
  <>
    {/* <TextField> </TextField> из material-ui
<SubmitButton> из ui-kit
<Counter></Counter> из ui-kit */}
  </>
));
