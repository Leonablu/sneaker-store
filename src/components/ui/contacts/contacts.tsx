import { FC, memo } from 'react';
import styles from './contacts.module.css';

export const ContactsUI: FC = memo(() => (
  <>
    {/* форма из material ui 
<socialMediaButtons> </socialMediaButtons> ссылки на соц сети  */}
  </>
));
