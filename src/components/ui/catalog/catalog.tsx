import { FC, memo } from 'react';
import styles from './catalog.module.css';

export const CatalogUI: FC = memo(() => (
  <>
    {/* <Grid> из material ui
 выводим ProductCardUI
 </Grid>  */}
  </>
));
