import { FC, memo } from 'react';
import styles from './blog.module.css';
import { SubscriptionUI } from '../subscription';
import { BlogCardUI } from '../blog-card';

export const BlogUI: FC = memo(() => (
  <>
    {/* <BlogCardUI></BlogCardUI> 
<SubscriptionUI></SubscriptionUI> */}
  </>
));
